$(document).ready(function() {
  // $(document).ready() ensures that this script runs after
  // the HTML document has finished loading

  // update text color of header element
  $('header').css('color', '#FFOOOO');
});
